#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _atoi - convert string to number
 * @s: the string to be passed
 * Return: The integer conversion
 */
int _atoi(char *s)
{
	return (atoi(s));
}
